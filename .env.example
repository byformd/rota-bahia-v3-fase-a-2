# URL base da API do backend (ver /backend). Em produção, aponte para onde o backend
# estiver hospedado (ex: Railway, Render — Vercel serverless não é ideal para este
# backend com SQLite local, ver README > Deploy do Backend).
VITE_API_URL="http://localhost:4000/api"
